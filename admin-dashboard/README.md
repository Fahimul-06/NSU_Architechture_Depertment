# NSU Architecture Admin Dashboard

Connected administration interface for registering students with NFC cards, faculty/professors, and departments.

## Run
1. Start backend from ../backend with `npm start`.
2. Run `npm install` then `npm run dev` in this folder.
3. Open the Vite URL.

Set `VITE_API_URL` to the public Render backend URL for deployment.
